<div class="container">
  <h1> <%= @post.title %> </h1>
  <p><strong>$<%= @post.price %></strong></p>

  <table>
    <tr>
      <td>
        <%= @post.content %>
      </td>
    </tr>

    <tr>
      <td>
        <p> contact user at <%= @post.email %> </p>
      </td>
    </tr>
  </table>
</div>
